# miniz

Amalgamated miniz 3.0.2 (https://github.com/richgel999/miniz), MIT. zlib-compatible
deflate/inflate (`mz_deflate`/`mz_inflate`), CRC32/Adler32, and `mz_zip` archive
read/write. Consumed by `modules/compress` for the `deflate`/`gzip` codecs and the
zip archive surface.
