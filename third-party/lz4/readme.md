# lz4

LZ4 1.10.0 `lib/` sources (https://github.com/lz4/lz4), BSD-2-Clause: core block codec,
HC compressor, LZ4 frame format, and the bundled xxhash. Consumed by `modules/compress`
for the `lz4` codec (frame format, streaming).
