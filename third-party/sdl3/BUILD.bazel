package(default_visibility = ["//visibility:public"])

cc_library(
    name = "sdl3",
    linkopts = select({
        "//bazel/config:plat_win32": ["-lSDL3-static"],
        "//conditions:default": ["-lSDL3"],
    }),
    target_compatible_with = select({
        "//bazel/config:plat_wasm": ["@platforms//:incompatible"],
        "//conditions:default": [],
    }),
)
