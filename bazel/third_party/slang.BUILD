cc_library(
    name = "slang_runtime",
    srcs = glob(["lib/libslang-compiler.0.*.dylib"]),
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
