# allocator.heap todo

- consider exposing the active heap memory-debug policy in diagnostics/logging
- add focused tests for aligned heap realloc/free behavior without going through higher-level allocators
- revisit whether the default memory-debug budgets should become per-platform config macros
