package(default_visibility = ["//visibility:public"])

cc_library(
    name = "signal",
    srcs = glob(["src/*.c"]),
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    deps = [
        "//modules/core",
        "//modules/fiber",
    ],
)
