package(default_visibility = ["//visibility:public"])

cc_library(
    name = "color",
    srcs = glob(["src/*.c", "src/*.h"]),
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    deps = [
        "//modules/math",
    ],
)
