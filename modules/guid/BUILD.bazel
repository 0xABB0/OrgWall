package(default_visibility = ["//visibility:public"])

cc_library(
    name = "guid",
    srcs = glob(["src/*.c"]),
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    deps = [
        "//modules/core",
        "//modules/hash",
        "//modules/string",
    ],
)
