package(default_visibility = ["//visibility:public"])

cc_library(
    name = "math",
    srcs = glob(["src/*.c"]),
    hdrs = glob(["include/**/*.h"]),
    textual_hdrs = glob(["include/**/*.inl"]),
    includes = ["include"],
    deps = [
        "//modules/core",
        "//third-party/mpfr",
    ],
)
