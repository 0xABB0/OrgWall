#+TITLE: GUI — TODO

Execution checklist for the GUI module. =readme.org= is the specification;
this file is what remains to do and the resume point for the next agent.

Status keys: TODO (not started), WIP (in progress). Completed work has been
removed; the shipped design lives in =readme.org=.

* Open items from shipped work

** Inversion lifecycle
- =on_destroy= no longer fires from the inverted backends (it used to come from
  the generic node->cb in =destroy_tree=). Re-wire per-backend from the native
  teardown if/when an app needs it. Unused by hello-world-gui today.

** Containers (Panel / GroupBox / ScrollView)
- GroupBox/ScrollView focus is the only capability wired on the content node;
  pointer/keyboard on the container surface ship on Panel (appkit) only.

** Structural (Dialog / TabView / Splitter)
- TabView/Splitter page nodes are grandchildren of the frame, so the shallow
  one-level =mel_gui__destroy_tree= leaks their node slots (and, on appkit, the
  page host global ref) when the frame closes — a pre-existing limitation of the
  destroy walk that also affects nested Panel/GroupBox children.

** Insets / safe area
- *PAD width/height reduction in landscape.* Android =PAD= pads the Frame's
  =FrameLayout=, which honours it for child positioning, so portrait
  (status top / nav bottom) is correct. But the C column layout still arranges
  against the *full* Frame size, so a left/right inset (landscape nav bar, side
  notch) shifts children right without shrinking the arranged width — content can
  clip. Fix: feed =safe_area= into the layout as an outer margin (reduce
  available size + offset origin) rather than Java padding; folds into the WIP
  layout system.
- *appkit PAD child-offset.* For normal decorated windows =safeAreaInsets= is 0,
  so PAD is a visual no-op. Real child-offset only bites under
  full-size-content / custom-titlebar windows, which the desktop class tier
  introduces — wire PAD offset there.
- *Typed extras as escape hatches.* status-vs-navigation split,
  =tappableElement=, =waterfall= (Android); =keyboardLayoutGuide=-driven =ime=
  on appkit/uikit; web =env(safe-area-inset-*)= + =visualViewport= for =ime=.

* Deferred — backends

Each is specified in =readme.org= and must be delivered first-class.

** TODO uwp / WinUI backend (declarative; observable-model bridge)
** TODO swiftui backend (macOS/visionOS ornaments & volumes; declarative)
** TODO gtk4 backend (Linux default; portable to win32/macos)
** TODO qt backend (portable; C++ shim TU)
** TODO compose backend (Jetpack Compose; declarative; JNI/Kotlin bridge)
** TODO drawn backend (CPU + GPU renderers; immersive Quest & visionOS)

* Deferred — android follow-ups

Do not block the android backend; tracked for later.

- Modified-UTF-8 vs standard UTF-8 at the JNI text boundary.
  =mel_gui__android_jstring= uses =NewStringUTF= and the inbound paths
  (=backend_get_text=, =MelTextField.nativeTextChanged=) use
  =GetStringUTFChars=; both speak JNI *modified* UTF-8, not standard UTF-8.
  Correct for ASCII and the whole BMP, mis-encodes supplementary characters
  (emoji, U+10000+). Fix all three paths together — outbound via =NewByteArray=
  + =SetByteArrayRegion= + =new String(bytes, "UTF-8")=, inbound via
  =String.getBytes("UTF-8")= → =byte[]= — caching the =java/lang/String= class,
  the =([BLjava/lang/String;)V= ctor and the =getBytes(Ljava/lang/String;)[B=
  method id once. Half-fixing one direction is incoherent.
- Drain UI events / wake from C: the reactor's android backend uses its own
  timerfd to schedule ticks; views are manipulated synchronously from C on the
  UI thread, which is correct, but =mel_reactor_post= from a non-UI thread
  should wake the looper — verify =ALooper_wake= path.
- =keyDown:= forwarding for =EditText= — Android consumes most key events in the
  IME, so on_key_* on a textfield won't fire on Android today.
- Per-backend escape hatches: =<gui/android/*.h>= class-tier headers exposing
  raw =android.view.View= access beyond the core API.
- =savedInstanceState= / =Bundle= state preservation. Today if the OS kills the
  process under memory pressure, all C-side state is lost and the app restarts
  at the root screen. Real Android apps serialize the current screen + the
  user-visible bits of session state into a Bundle in =onSaveInstanceState= and
  restore in =onCreate=. Requires Melody apps to declare what state is
  screen-saveable.
- Activity-result / intent system. No way today to launch a system activity
  (camera, share, file picker) and receive a result. The readme calls
  multi-window / native-menus first-class but doesn't yet name intent dispatch;
  the mobile class tier should.
- Custom-Android-View extension story. An app that wants a widget Android has no
  native equivalent for (e.g. a music keyboard) must today add code to
  =modules/gui/src/android/=. There is no out-of-tree extension point: a Melody
  app should be able to register its own =android.view.View= subclass as a
  widget kind without touching the gui module's android backend sources.
- Opt-in multi-Activity =mel_app_present= mode. Today every Screen is a sibling
  in one Activity. Some apps (deep linking, intent return, per-screen task
  isolation) want each Screen pushed as its own Android Activity. A
  =MEL_FRAME_OWN_ACTIVITY= flag on =Mel_Frame_Opt= would switch
  =mel_app_present= for that frame to =startActivity=, default stays
  single-Activity. Gated on the mobile class tier.

* Deferred — class-tier headers

** TODO desktop/ class tier — resizable, decorations, multi-window, monitor placement
** TODO mobile/ class tier — back stack, scenes, lifecycle hooks
** TODO web/ class tier
** TODO xr/ class tier — volumes, ornaments, immersive spaces

* Deferred — widgets beyond the first slice

** TODO RadioButton, ToggleButton, ProgressBar, SpinBox
** TODO ChoiceBox, ComboBox, ListBox, ImageView, TextArea
** TODO MenuBar / Menu / MenuItem, ToolBar, StatusBar
** TODO Drawn-widget family (mel_draw_*) — finalise its vocabulary when built

* Deferred — subsystems

** WIP  Layout system
   Shipped: extracted to =modules/layout/= (items-based solvers, open
   =Mel_Layout_Class=, no gui dependency, unit-tested); column + row
   (=linear=) and stack kinds; node sibling lists (fixes the O(all-nodes)
   child scan and slot-reuse ordering); =container_arrange= replacing the
   per-backend fake-vtable layouts; lowering hooks with dom (flexbox +
   single-cell-grid stack + ResizeObserver size mirroring) and androidnative
   (=LinearLayout=, divider-drawable spacing) adopting =linear=;
   =mel_gui__backend_natural_size= on cocoa/uikit/android.
   Open items:
   - =Mel_Grid_Layout= — 2D with row/col spans + stretch factors per track.
   - Stack per-child alignment (today: pure fill-overlay; dom grid items
     always stretch).
   - Un-lowering: =mel_gui_set_layout(parent, NULL)= (or swapping to an
     unrecognised class) clears =lowered= but leaves native engine state —
     dom keeps flex inline styles on host and children; android keeps (and on
     re-lowering nests) the injected =LinearLayout=. Needs a per-backend
     un-adopt.
   - Lowered-state C mirror: dom mirrors only w/h (async, frame-batched;
     x/y stay 0 → =content_size= under-reports); android does not mirror
     bounds under a lowered parent at all.
   - Natural size on dom (no cheap reliable measurement found) and win32
     (=GetTextExtentPoint32= per control font); android =View.measure= on an
     attached view briefly perturbs its measured state.
   - NSStackView (cocoa) / UIStackView (uikit) lowering.
   - cocoa tab pages: =container_arrange= updates the C mirror but does not
     push bounds (NSTabView owns the host frames) — unchanged from the old
     vtable, worth a second look with lowering.

** WIP  Styling (per-component)
   Shipped: per-widget =Mel_<Widget>_Style= structs composed from the
   =<gui/style.h>= primitives (=Mel_Style_Color=, =Mel_Font=,
   =Mel_Style_Surface=), =.style= in every widget opt except gpu_view
   (deliberate), per-widget =mel_<widget>_set_style= ops on all six backends,
   applied from every create. Widget-specific knobs: slider =track/thumb=,
   checkbox =tint=, groupbox =title_font/title_fg=, splitter =divider=.
   Native first; per-property emulation only (cocoa/uikit layer, win32
   =SetWindowRgn=, android =GradientDrawable=, dom inline CSS).
   Open items:
   - Reset-to-native: set_style applies set fields only; nothing restores a
     native look (e.g. an NSButton whose bezel was dropped for bg/radius
     stays bezel-less).
   - win32: push-button fg/bg needs =BS_OWNERDRAW= (font + region apply
     today); colored borders need =WM_NCPAINT=; trackbar colors are
     comctl-owner-drawn; comctl v6 themes may override checkbox/groupbox
     caption colors; *the whole winui style/layout code is compile-unverified
     — the win-pilot box was unreachable this session* (build
     =worktree-gui-style-layout= there first thing).
   - cocoa: padding has no native knob outside =NSBox= (l/t only); NSBox
     title color; NSWindow styles only bg; NSSlider only track fill (inert
     before 10.12.2); splitter divider color; per-tab-label styles.
   - uikit: padding only on UIButton (=contentEdgeInsets=, deprecated-at-15
     but the targeted floor is 13); UISwitch/UISlider style only their tints.
   - android: a styled bg replaces the platform ripple/elevation drawable;
     font weights collapse to BOLD/NORMAL before API 28; style padding and
     =MODE_PAD= inset padding overwrite each other on frames; the hard-coded
     factory colors (MelLabel/MelCheckBox/MelTextField =0xE9EEF5= text,
     groupbox border) pre-empt the "unset = native default" promise —
     revisit.
   - xcb: only =CW_BACK_PIXEL= bg (TrueColor assumption); logged once.
   - New per-widget gaps: slider thumb on dom (needs injected
     ::-webkit-slider-thumb rules) and cocoa (no thumb API); checkbox tint on
     cocoa (no glyph API) and win32 (theme-drawn); checkbox fg/font on uikit
     (bare UISwitch has no label); groupbox title_fg on cocoa (NSBox has no
     title color) and title entirely on uikit (plain view); splitter divider
     everywhere except android (native paint) and win32 (bg-brush nuance:
     divider and surface.bg are the same physical surface there).
   - Theming: an app-wide per-widget-kind style table resolved at create
     time (no retained cascade).

** WIP  Unified Canvas drawing API
   Shipped: =Mel_Painter= immediate-mode surface handed to Canvas =on_paint=.
   Ops: =clear=, =fill_rect=, =fill_ellipse=, =stroke_rect=, =draw_line=,
   =fill_round_rect=, =draw_text=. Geometry reuses =math.geo=/=math.vector=.
   *Extracted to the =paint= module, all backends migrated.* =Mel_Painter=, the
   seven ops, and the color type now come from =<paint/painter.h>= +
   =<color/rgba8.h>= (=mel_color8=); =gui= =mel_depends= on =paint=+=color=. Every
   canvas vends a borrowed =Mel_Drawable= (=mel_drawable_borrow=/=release=) from its
   paint entry: cocoa/uikit =drawRect= (=CGContextRef=), winui =WM_PAINT= (=HDC=),
   dom =canvas_repaint= (element id), android =nativePaint=
   (=Mel_Paint_Android_Native=). Retired: =gui/painter.h=, =gui/color.h=, the
   per-backend =painter.{m,c}= (cocoa/uikit/winui/androidnative), the inline dom
   painter, every private =struct Mel_Painter=, =mel_gui__android_painter_register_jni=.
   Verified: cocoa live; ios/wasm/android build; win32 backend compiles (nob win32
   toolchain pre-broken). Android boot still blocked by the pre-existing JNI
   whole-archive gap (gui's =Java_*= entry points GC'd from =libmelody.so=) — see
   =modules/paint/todo.md=.
   Open items / deferred surface:
   - Arbitrary paths (move/line/curve/close, fill + stroke), polygons.
   - Images / bitmaps (=draw_image=), with source + dest rect and scaling.
   - Clip regions (rect + path) and affine transforms (translate/scale/rotate).
   - Gradients (linear/radial) and per-shape alpha/blend modes.
   - Text: only a single run with the system font + size ships; rich text,
     font family selection, measurement (=measure_text=), alignment and
     wrapping are deferred. Android/win32 text goes through JNI modified-UTF-8 /
     UTF-16 — same supplementary-char caveat as the other text paths.
   - Drawn backend: implement =mel_painter_*= by recording into the shared
     draw-list its CPU/GPU renderers play back.

** TODO Per-backend accessibility bridges
   UI Automation (win32/uwp), NSAccessibility (appkit/uikit), AccessKit-style
   for the drawn backend. First slice relies on native control a11y defaults.

** TODO --gui-backend selector
   Override the compiled =src/<backend>/= directory and define
   =MEL_GUI_BACKEND_<NAME>=; default per platform. Enables e.g. gtk4 on win32.

** TODO Migrate Mel_Key / Mel_Mouse_Button to the PRD 09 Input module
   Remove the provisional =<gui/input.h>= definition once that module exists.

** TODO Per-callback-slot setters
   PRD 05 wants a setter per individual callback slot
   (=mel_gui_set_pointer_on_click= and friends). None ship today — the
   per-capability bulk setters (=mel_gui_set_*_cb=) were removed as unused and
   type-erasure-hostile under native-stored callbacks. Add per-backend when
   needed.

** TODO Win32 opt-in polish APIs
   =<gui/win32/...>=: Common Controls v6 visual styles, default-font override,
   per-monitor DPI awareness — opt-in only, never defaults.

** TODO Rename =src/macos/= → =src/appkit/=
   Aligns the source dir with the backend name used in =readme.org= and with
   the public include path =<gui/appkit/...>=. Gated on the =--gui-backend=
   selector landing so swiftui can coexist on macOS.

** TODO Automated callback tests
   Gated on the PRD 09 input.replay module. Per PRD 05: callback fires on
   event, NULL slots skipped, runtime setters change dispatch, lazy allocation
   survives stress.

** WIP  Screen / Navigation model
   Shipped (see =readme.org= "Frames, Screens, Navigators and Roots"): identity
   registry (=screen.c=) split from instance stacks (=nav.c=); Navigator per Root;
   =present= (new Root) + =push=/=replace=/=back=/=pop_to[_root]=; per-nav =arg=;
   =on_enter=/=on_leave=/=on_destroy= hooks; =mel_gui_supports_multi_root=;
   =present= degrade (mobile push / web route); OS-back reconciliation
   (=frame_closed=, iOS =didMoveToParentViewController=, android =nativeOsBack=,
   web =popstate=); =present_root= window-sizing moved out of the portable layer.
   Cocoa runtime-verified; android APK builds; ios/wasm build-blocked on this host.
   Open items:
   - *iOS swipe leaks the node.* =frame_closed= reconciles the stack but must not
     destroy the frame (cocoa/winui destroy it themselves), so an iOS swipe pop
     leaves the popped VC's =Mel_Gui_Node= alive. Destroy it from the iOS pop path
     specifically once iOS is testable.
   - *Web URL leads the stack by one.* =mel_app_back= advances without
     =history.back=, so the route can be one entry ahead. Browser back still pops
     correctly; full bidirectional sync is a refinement.
   - *Multi-root opt-in escape hatches.* iPad =UIWindowScene= / Android own-task
     (=MEL_FRAME_OWN_ACTIVITY=) / web real =window.open=. Default degrade ships;
     these need a =Mel_Frame_Opt= flag plumbed.
   - *Process-death restore.* Contract decided (args not preserved by default;
     names rebuild with =arg = NULL=). Optional per-screen =arg_encode/arg_decode=
     and the =savedInstanceState=/scene-restoration plumbing are unbuilt.
   - *Retire the cap enum.* =mel_gui_backend_supports(Mel_Gui_Capability)= (enum,
     =gui.c=) predates the predicate form; fold =MULTI_WINDOW=/=NATIVE_MENUS= into
     per-backend predicates like =mel_gui_supports_multi_root= and drop the enum
     (MEL-CODE-001).
