package(default_visibility = ["//visibility:public"])

cc_library(
    name = "ioasync",
    srcs = glob(["src/*.cc"]),
    hdrs = glob(["include/**/*.hh"]),
    includes = ["include"],
    deps = [
        "@stdexec//:stdexec",
    ],
)
