package(default_visibility = ["//visibility:public"])

cc_library(
    name = "test",
    srcs = glob(["test/*.c"]),
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    deps = [
        "//modules/core",
    ],
)
