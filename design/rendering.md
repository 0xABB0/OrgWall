# Rendering:

Rendering is split into two different kinds: Low level and High level.

note: this is separate from the gpu api, that are even lower level than this module (this module builds on top of that)

## High Level:

### Draw api

## Low Level:

- RenderSet
- RenderPath(?)
- Geometry
- Material
